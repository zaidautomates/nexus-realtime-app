# Server Configuration
PORT=5000

# JWT Secret — change this to a long random string in production
JWT_SECRET=nexus_comm_secret_key_change_this_in_production

# AES-256-GCM Encryption Key — change this to a long random string in production
ENCRYPTION_KEY=nexus_comm_encryption_key_change_this_in_production
